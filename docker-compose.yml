version: '3.8'
services:
  jenkins:
    build:
      .
    ports:
        - 8080:8080
    env_file:
      - .env
    volumes:
      - ./jenkins.yaml:/var/jenkins_home/jenkins.yaml
      - ./job_dsl.groovy:/job_dsl.groovy